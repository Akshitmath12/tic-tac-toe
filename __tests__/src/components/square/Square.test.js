// Test generated by RoostGPT for test reactTesting using AI Type Open AI and AI Model gpt-4-1106-preview

import React from "react";
import { render, screen, fireEvent } from "@testing-library/react";
import Square from "../../../../src/components/square/Square";

// Test suite for the Square component
describe("Square", () => {
  // Test case for rendering the Square component with default props
  test("renders Square with default props", () => {
    render(<Square value='X' onClick={() => {}} />);
    const buttonElement = screen.getByRole("button", { name: "X" });
    expect(buttonElement).toBeTruthy();
  });

  // Test case for simulating a click event
  test("calls onClick prop when clicked", () => {
    const onClick = jest.fn();
    render(<Square value='X' onClick={onClick} />);
    const buttonElement = screen.getByRole("button", { name: "X" });
    fireEvent.click(buttonElement);
    expect(onClick).toHaveBeenCalledTimes(1);
  });
});
